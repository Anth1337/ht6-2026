{
  "name": "ticketmaster-demo",
  "private": true,
  "version": "0.1.0",
  "description": "Demo ticket storefront (not affiliated) that hands off to SunPay",
  "scripts": {
    "dev": "node --env-file=.env server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
